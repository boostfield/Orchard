@model Orchard.Xmu.Models.ENCoursePart
@{ 
    Style.Require("Select2");
    Script.Require("Course").AtHead();
    var teachers = Model.Teachers;
}
<fieldset>
    @Html.LabelFor(m=>m.CourseName)
    @Html.TextBoxFor(m => m.CourseName)
</fieldset>
<fieldset>
    @Html.LabelFor(m => m.CourseNO)
    @Html.TextBoxFor(m => m.CourseNO)
</fieldset>
@Html.Label("相关老师")
<select class="teacher-in-course" style="width: 400px;" multiple="multiple" name="ENCoursePart.TeacherIds">
    @foreach (var teacher in teachers)
    {
        @Html.SelectOption(teacher.Id, true, teacher.ENName + teacher.SN);
    }
</select>
